SELECT NAME FROM STUDENTS WHERE MARKS>75 ORDER BY SUBSTRING(NAME,LENGTH(NAME)-2,LENGTH(NAME)), id;


-- MII
SELECT name FROM students
WHERE marks > 75
ORDER BY RIGHT(name,3), RIGHT(name,2), RIGHT(name,1), id;
